# reto3-backend

CRUD con NodeJS y MongoDB (mlab)

Clonar repositorio:
    
    luego instalar dependecias:
    npm install
      
    Ejecutar:
        node server.js
  
    Abrir navegador en :
        localhost:3000
